[pytest]
# pytest-asyncio: async fixtures default to a function-scoped event loop.
# No async fixtures exist today; set explicitly to avoid deprecation noise.
asyncio_default_fixture_loop_scope = function
